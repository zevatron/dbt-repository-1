version: 2

sources:
  - name: stripe
    description: Replica of the database
    database: raw
    schema: stripe
    tables:
      - name: payment
        description: One record per payment
